NEXT_PUBLIC_API_URL=http://localhost:4000

# Optional override for Google Sign-In. If omitted, frontend reads GOOGLE_CLIENT_ID from ../backend/.env (same repo layout).
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=
